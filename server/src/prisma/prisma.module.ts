import { Module } from '@nestjs/common';
import { PrismaService } from './prisma.service';

@Module({
  providers: [PrismaService],
  exports: [PrismaService], // important so other modules like ApiKeyModule can use it
})
export class PrismaModule {}
